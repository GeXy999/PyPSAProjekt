# Abhängigkeiten für Streamlit Community Cloud / frische Installationen.
# Installieren:  pip install -r requirements.txt

streamlit
plotly
pandas
numpy
matplotlib
scipy
pypsa
highspy
openpyxl        # nur für den XLSX-Upload nötig; CSV läuft ohne

# Optionaler Premium-Solver: wird nur genutzt, wenn eine gültige Gurobi-WLS-
# Lizenz als Secret/.env hinterlegt ist (GRB_WLSACCESSID/-SECRET/-LICENSEID);
# sonst ignoriert das Modell ihn und rechnet mit HiGHS.
gurobipy

# Fuer die optionale KI-Zusammenfassung (lokal via Ollama, HTTP-API auf
# localhost:11434). Meist schon als Transitiv-Abhaengigkeit vorhanden,
# hier zur Klarheit explizit gelistet.
requests

# atlite (ERA5-Wetter) bewusst NICHT enthalten: sehr schwer und der ERA5-Download
# ist auf dem kostenlosen Cloud-Tier unpraktikabel → ohne atlite synthetisches
# Wetter. Für echte ERA5-Daten atlite + cdsapi lokal installieren.

# Ollama selbst ist KEIN Python-Paket und wird NICHT hier installiert:
# separat von https://ollama.com installieren (lokale App), danach z. B.
# "ollama pull llama3.2:3b" in einem Terminal. Ohne Ollama laeuft die App
# unveraendert weiter, nur der KI-Zusammenfassungs-Expander bleibt inaktiv.
